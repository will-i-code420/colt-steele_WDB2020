// use querySelector and querySelectorAll to do the following
// 1) select all elements with class of done into a variable doneTodos
// 2) select one checkbox and save it to a variable called checkbox
//    use the type attribute

const doneTodos = document.querySelectorAll('.done');
const checkbox = document.querySelector('input[type="checkbox"]');
