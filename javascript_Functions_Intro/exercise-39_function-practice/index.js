// write a function called isShortsWeather that will accept a single
// number argument.  If this argument is >= 75 return true otherwise false

function isShortsWeather(temp) {
  if (temp >= 75) {
    return true;
  } else {
    return false;
  }
}
